# pretraining dataset

#_target_: slm.data.protein_datamodule.ProteinDataModule
_target_: slm.data.protein_datamodule_var.ProteinDataModule

dataset_var:
  #_target_: slm.data.protein_datamodule.ESMEmbeddingDataset
  _target_: slm.data.protein_datamodule_var.ESMEmbeddingDataset
  path_to_text_list: /data/pdb_data/processed_chains/dssp_success_short_256.txt
  # path_to_text_list: /data/pdb_data/processed_chains/dssp_success_filtered_756.txt
  path_to_embeddings: /data/structure_preprocess
  path_to_sequences: /data/pdb_data/processed_chains_encoding
  cluster_rep_csv: null
  transform: null
  training: true
  max_len: 256


batch_size: 2   # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
generator_seed: 42
train_val_split: [0.95, 0.05]
num_workers: 4
pin_memory: false
shuffle: true
